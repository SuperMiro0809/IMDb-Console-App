#pragma once

constexpr int DEFAULT_COLOR = 0;

constexpr int DEFAULT_HEADER_LEFT_MARGIN = 3;
constexpr int DEFAULT_DIVIDER_LENGTH = 25;
constexpr char DEFAULT_DIVIDER_CHAR = '-';

constexpr int DEFAULT_TEXT_FIELD_LENGTH = 255;